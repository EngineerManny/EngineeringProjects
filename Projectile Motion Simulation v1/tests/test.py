import numpy as np

data_points = int(input("How many data points for height vs time data? "))

height_vs_time_array = np.zeros((data_points, 2), int)
print("Empty Array: ")
print(height_vs_time_array)

